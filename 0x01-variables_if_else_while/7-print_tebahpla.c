#include <stdio.h>

/**
 * main - i sometimes suffer from insomnia
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char A;

	for (A = 'z'; A >= 'a'; A--)
	{
	putchar(A);
	}
	putchar('\n');

	return (0);
}
